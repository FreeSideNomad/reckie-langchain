# ==========================================
# Database Configuration
# ==========================================
# PostgreSQL connection settings for LangChain Document Management System
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update POSTGRES_PASSWORD to a strong password
# 3. (Optional) Customize other values for your environment
# 4. NEVER commit .env file to git (already in .gitignore)
# ==========================================

# PostgreSQL Database Configuration
# ----------------------------------
# Database user (default: langchain_user)
POSTGRES_USER=langchain_user

# Database password (CHANGE THIS IN PRODUCTION!)
# Use a strong password with: uppercase, lowercase, numbers, special chars
POSTGRES_PASSWORD=change_me_in_production_123!

# Database name
POSTGRES_DB=langchain_docs

# Database host (use 'localhost' for local dev, 'postgres' for Docker network)
POSTGRES_HOST=localhost

# Database port (default PostgreSQL port: 5432)
POSTGRES_PORT=5432

# SQLAlchemy Database URL
# Format: postgresql://{user}:{password}@{host}:{port}/{database}
# This is auto-constructed from the variables above
# Used by: SQLAlchemy, Alembic migrations, FastAPI
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# For Docker internal networking (services communicate via service names)
# Uncomment if running FastAPI in Docker:
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}

# ==========================================
# OpenAI Configuration
# ==========================================
# OpenAI API key for LLM and embeddings
# Get your key from: https://platform.openai.com/api-keys
# Required for: GPT-4 conversations, text embeddings (RAG)
OPENAI_API_KEY=sk-proj-your-actual-key-here

# OpenAI Model Configuration (optional overrides)
# OPENAI_MODEL=gpt-4  # Default model for conversations
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small  # Default for embeddings
# OPENAI_TEMPERATURE=0.7  # Creativity vs consistency (0.0-1.0)

# ==========================================
# Application Configuration
# ==========================================
# Environment: development, staging, production
ENVIRONMENT=development

# Application host and port (for FastAPI)
APP_HOST=0.0.0.0
APP_PORT=8000

# Enable debug mode (verbose logging)
DEBUG=true

# Secret key for JWT tokens (generate with: openssl rand -hex 32)
SECRET_KEY=your-secret-key-here-change-in-production

# ==========================================
# Optional: Advanced Settings
# ==========================================
# Database connection pool settings
# DB_POOL_SIZE=20
# DB_MAX_OVERFLOW=10

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# ==========================================
# Notes
# ==========================================
# - All values with defaults will work out of the box for local development
# - MUST change: POSTGRES_PASSWORD, OPENAI_API_KEY, SECRET_KEY
# - Keep .env file secure and never commit to version control
# - See README.md for detailed setup instructions
